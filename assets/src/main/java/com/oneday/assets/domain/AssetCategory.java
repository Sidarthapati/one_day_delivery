package com.oneday.assets.domain;

/** Broad class of asset. Vans/scanners are SERIALIZED and returnable; PACKAGING is BULK stock (reserved). */
public enum AssetCategory {
    VEHICLE, SCANNER, DEVICE, BAG, PACKAGING, OTHER
}
