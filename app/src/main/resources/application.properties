spring.application.name=one-day-delivery

# Database — defaults to local; override the env vars to point at the shared Render cluster.
# Real values live in .env (gitignored), loaded with: set -a && source .env && set +a
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/oneday}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:oneday}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:secret}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
# Apply a lower-version migration even if a higher one already ran — needed on the shared
# Render DB where M5 (V5_x) and M6 (V6_x) migrations merge out of chronological order.
spring.flyway.out-of-order=true

# Kafka — ON. Provide KAFKA_* env vars (see .env) with SPRING_PROFILES_ACTIVE=dev to reach
# the Confluent cluster; defaults to localhost:9092 otherwise. To run Postgres-only without
# a broker, re-add: spring.autoconfigure.exclude=...kafka.KafkaAutoConfiguration
kafka.enabled=true

# JWT
jwt.secret=change-me-in-production-must-be-at-least-32-characters-long
jwt.expiry-hours=8

# Logging
logging.level.com.oneday=INFO
